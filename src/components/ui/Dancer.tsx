import { styled } from "styled-components"

export const Dancer = () => {
  return <Root></Root>
}

const Root = styled.div`
  width: 32px;
  height: 32px;
  background-color: hotpink;
  border-radius: 50%;
`
